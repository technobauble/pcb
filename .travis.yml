language: c

compiler: gcc

os:
  - linux
  - osx

branches:
  - master
  - travis-ci
  - /^pcb-.*-RELEASE/
  - /^LP.*/
  - /home\/cparker\/.*  

addons:
  apt:
    packages: gettext autopoint intltool

before_script:
  - ./autogen.sh

script:
  - ./configure --disable-doc --disable-dbus
