language: c

compiler: gcc

os:
  - linux
  - osx

branches:
  - master
  - /^pcb-.*-RELEASE/
  - /^LP.*/
  - /home\/cparker\/.*  

addons:
  apt:
    packages: gettext autopoint

before_script:
  - ./autogen.sh
